<nav>
    <div class="nav-wrapper">
        <a href="/" class="brand-logo">Hello <% if ( user ) { %><%= user.name %>,<%}%> welcome to Data To See</a>
        <ul id="nav-mobile" class="right hide-on-med-and-down">
            <% if ( !isAuthenticated ) { %>
            <li class="nav-item"><a href="/login">Login</a></li>
            <% } %>
            <% if ( user && (user.groups.indexOf("Admin") > -1  || user.groups.indexOf("Therpaists") > -1)) { %>
                <li class="nav-item"><a href="/listUsers">My Users</a></li>
            <li class="nav-item"> <a href="/listQuestionare">Questionare</a></li>
            <li class="nav-item"> <a href="/csv">Upload CSV</a></li>
            <li class="nav-item"> <a href="/manualTest">Calculate Tests</a></li>
            <li class="nav-item"> <a href="/produceReport">Report</a></li>
            <li class="nav-item"><a href="/sendEmail">send Email</a></li>
            <% }%>
            <% if ( isAuthenticated ) { %>
                <li class="nav-item"> <a href="/mail">Contact Us</a></li>
                <li class="nav-item"><form method="get" action="/logout"> <!--change to post when launch website so that the user is not remembered!-->
                    <button id="logout-button" style="border:none;
                        outline:none;
                        background:none;
                        cursor:pointer;
                        color:#FFFFFF;
                        padding:0;
                        font-family:inherit;
                        font-size:inherit;
                    " type="submit">Logout</button>
                </form></li>
            <% } %>
        </ul>
    </div>
</nav>