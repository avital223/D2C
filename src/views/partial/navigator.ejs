<nav>
    <div class="nav-wrapper">
        <a href="/" class="brand-logo">Hello <% if ( user ) { %><%= user.name %>,<%}%> welcome to Data To See</a>
        <ul id="nav-mobile" class="right hide-on-med-and-down">
            <% if ( isAuthenticated ) { %>
            <li class="nav-item"><form method="get" action="/logout"> <!--change to post when launch website so that the user is not remembered!-->
                <button id="logout-button" style="border:none;
                    outline:none;
                    background:none;
                    cursor:pointer;
                    color:#FFFFFF;
                    padding:0;
                    font-family:inherit;
                    font-size:inherit;
                " type="submit">Logout</button>
            </form></li>
            <% } %>
            <% if ( !isAuthenticated ) { %>
            <li class="nav-item"><a href="/login">Login</a></li>
            <% } %>
            <% if ( user && user.groups.indexOf("Admin") > -1 ) { %>
            <li class="nav-item"> <a href="/listQuestionare">Questionare</a><ul>
            <li class="nav-item"><a href="/questionare">Add new Questionare</a></li>
            </ul>
            </li>
            <% } else { if(user){ %>
                <li class="nav-item"> <a href="/listMyQuestionare">My Questionares</a></li>
            <%  } } %>
        </ul>
    </div>
</nav>